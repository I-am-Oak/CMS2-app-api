global:
  scrape_interval: 15s
  evaluation_interval: 15s
  # rule_files:
  #   - "first.rules"
  #   - "second.rules"

scrape_configs:
  - job_name: monitoring
    static_configs:
      - targets:
          - 172.21.32.1:8000
